import userModel from './user';
import buyerModel from './buyer';
import leadModel from './lead';
import driveModel from './drive';
import emailModel from './email';
import stageModel from './stage';

const collections = {
  userModel,
  buyerModel,
  leadModel,
  driveModel,
  emailModel,
  stageModel
};

export {
  userModel,
  buyerModel,
  leadModel,
  driveModel,
  emailModel,
  stageModel
};

export default collections;
